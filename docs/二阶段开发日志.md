# 二阶段开发日志

## 2026-06-26：足球助手 UI 与聊天接口骨架

### 本次目标

- 将原前端 AI 气泡入口改造为足球形态的校园助手入口。
- 展开后显示统一聊天面板。
- 面板底部提供能力组件，组件用于切换助手能力，不做页面跳转。
- 新增统一聊天接口骨架，为后续 LangGraph / LangChain / RAG / MCP 工具接入预留结构。

### 已完成

1. 前端 `AIAssistant.vue` 重构为足球入口。
2. 增加校园助手面板：
   - 顶部显示当前能力说明。
   - 中部为聊天消息流。
   - 底部为能力切换按钮。
   - 输入框支持 Enter 发送、Shift+Enter 换行。
3. 能力组件第一版包含：
   - 自动
   - RAG知识问答
   - 教务查询
   - 学习辅导
   - 文档处理
   - 情绪陪伴
   - 数据分析
   - 世界杯问答
   - AI知识问答
4. 新增后端接口：
   - `GET /api/v1/campus-agent/modes`
   - `POST /api/v1/campus-agent/chat`
5. 前端新增 API：
   - `getCampusAgentModes`
   - `chatWithCampusAgent`
6. 当前接口返回统一结构：
   - `session_id`
   - `reply`
   - `mode`
   - `intent`
   - `tool_calls`
   - `references`
   - `suggested_questions`

### 验证结果

- 后端 `py_compile` 通过。
- 前端 `npm run build` 通过。
- 浏览器验证通过：
  - 登录后右下角显示足球入口。
  - 点击足球可展开校园助手面板。
  - 9 个能力组件正常显示。
  - 点击“教务查询”后仅切换助手能力，不发生页面跳转。
  - 输入“查看我的考勤”可发送并收到统一接口响应。

### 当前说明

- 本次只完成 UI 和统一接口骨架。
- 真正的 RAG、教务工具调用、学习辅导、文档处理、数据分析等能力尚未正式接入。
- 当前 `campus-agent/chat` 先复用旧 `ai_service.chat`，并增加了兼容兜底，避免旧服务内部 Decimal 等数据格式问题影响新助手入口体验。

### 下一步建议

1. 新建 CampusAgent service 层，逐步脱离旧 `ai_service`。
2. 设计 `agent_session`、`agent_message`、`agent_tool_call` 等表。
3. 先接入教务工具调用：
   - 我的课表
   - 我的成绩
   - 我的请假
   - 我的考勤
   - 最近公告
4. 再接入 RAG 知识问答和 AI 知识问答。
5. 最后接入 LangGraph 编排、Deep Agents 文档处理、MCP 示例工具。

## 2026-06-26：足球助手升级为动态 3D 入口

### 本次目标

- 将足球助手入口从平面样式升级为动态、立体的足球。
- 保持原有点击展开、拖拽移动、能力切换和聊天面板逻辑不变。
- 避免 3D 依赖直接撑大主布局首屏包。

### 已完成

1. 前端新增 `three` 依赖，用于渲染 3D 足球。
2. `AIAssistant.vue` 的足球入口改为 `canvas` 渲染：
   - 使用 `SphereGeometry` 作为足球主体。
   - 使用五边形黑色贴片和环形缝线模拟足球纹理。
   - 增加环境光、主光和边缘光，让足球具备立体明暗。
   - 使用 `requestAnimationFrame` 持续旋转，面板展开时旋转更明显。
3. 采用 `await import('three')` 动态加载 Three.js：
   - 主布局包保持轻量。
   - Three.js 被拆成独立懒加载 chunk。
4. 增加 CSS 降级方案：
   - 浏览器不支持 WebGL 或 Three.js 加载失败时，仍显示带阴影和旋转效果的足球样式。
5. 组件销毁时释放 Three.js renderer、geometry、material 和动画帧，避免内存泄漏。

### 验证结果

- 前端 `npm run build` 通过。
- 构建结果中 `three.module-*.js` 独立拆包，`MainLayout` 未被 3D 依赖明显撑大。
- 当前仍存在 Vite/Rolldown 对第三方库 `/* #__PURE__ */` 注释的警告，以及项目既有大 chunk 警告；这些不是本次 3D 足球改造导致的阻断错误。

### 后续建议

1. 等校园助手后端能力稳定后，再考虑给足球加入状态反馈：
   - 空闲：慢速旋转。
   - 思考中：加快旋转或轻微跳动。
   - 报错：短暂红色描边。
2. 如果后续需要更真实的足球纹理，可以引入贴图资源，但当前阶段用几何贴片更轻量、也更容易维护。

## 2026-06-26：足球入口调整为参考图同款配色

### 本次目标

- 根据用户提供的世界杯风格足球图片，将助手入口从传统黑白足球改为白底、红绿蓝三色面板的现代足球。
- 保持动态 3D 旋转，不直接使用截图作为静态图片。

### 已完成

1. 使用程序生成 `CanvasTexture` 作为 Three.js 球体纹理。
2. 纹理包含：
   - 白色皮革底色。
   - 红、绿、蓝三组大弧形面板。
   - 白色分割弧线。
   - 细线纹理、星形点缀和简化文字标记。
3. 保留光照、球体旋转和面板展开时的动态速度变化。
4. CSS 兜底足球同步改为红绿蓝世界杯风格，避免 WebGL 不可用时回退成黑白足球。

### 验证结果

- 前端 `npm run build` 通过。
- Three.js 仍保持动态懒加载，`three.module-*.js` 独立拆包。

## 2026-06-26：足球助手自然语言 CRUD 第一步

### 本次目标

- 先完成自然语言 CRUD 的安全开发流程规范。
- 先建立工具白名单和权限映射，不直接开放危险写操作。
- 让足球助手能知道当前账号权限范围内可以调用哪些系统工具。

### 已完成

1. 在 `docs/二阶段开发计划文档.md` 中新增“足球助手自然语言 CRUD 开发流程规范”：
   - 白名单工具。
   - 后端硬权限。
   - 数据范围控制。
   - 高风险二次确认。
   - 工具调用审计。
2. 新增 `app/services/campus_agent/registry.py`：
   - 定义 `AgentTool`。
   - 登记第一批工具白名单。
   - 为每个工具绑定权限码、风险等级和确认要求。
3. 新增 `app/services/campus_agent/executor.py`：
   - 统一校验工具是否登记。
   - 统一校验当前用户是否具备工具权限。
   - 对需要确认的工具返回 `confirm_required`，不直接执行。
   - 第一阶段未接 handler 的工具返回安全提示。
4. 更新 `app/api/v1/campus_agent.py`：
   - `GET /api/v1/campus-agent/tools` 返回当前用户可用工具。
   - `GET /api/v1/campus-agent/modes` 在教务查询能力中附带可用工具。
   - 在教务查询模式下询问“你能做什么/工具清单/我的权限”等问题时，返回当前账号可用工具列表。

### 第一批工具白名单

| 工具编码 | 功能 | 权限码 | 风险 |
| --- | --- | --- | --- |
| query_my_schedule | 查询我的课表 | `schedule:my:list` | low |
| query_my_attendance | 查询我的考勤 | `attendance:my:list` | low |
| query_my_leave | 查询我的请假 | `leave:request:list` | low |
| create_leave_request | 提交请假申请 | `leave:request:create` | medium |
| query_announcements | 查询公告 | `announcement:list:view` | low |
| query_student | 查询学生 | `people:student:list` | low |
| query_teacher | 查询教师 | `people:teacher:list` | low |
| query_course | 查询课程 | `teaching:course:list` | low |
| query_class | 查询班级 | `org:clazz:list` | low |

### 验证结果

- 后端 `py_compile` 通过。
- 前端 `npm run build` 通过。
- 当前仍只完成工具白名单和权限映射，真实查询 handler 会按工具逐个接入。

### 下一步建议

1. 优先接入低风险查询工具：
   - 我的课表。
   - 我的考勤。
   - 我的请假。
   - 公告查询。
2. 再接入管理类查询：
   - 学生查询。
   - 教师查询。
   - 班级查询。
   - 课程查询。
3. 最后接入创建、修改、删除，并强制二次确认和审计日志。

## 2026-06-26：足球助手低风险查询闭环开发

### 本次目标

- 让足球助手从“展示可用工具”进入“自然语言触发真实查询”的阶段。
- 第一批只接入低风险查询，不开放删除和批量修改。
- 优先复用现有 service 的权限范围控制，确保聊天入口和页面入口的数据边界一致。

### 本轮开发范围

1. 接入 `query_my_schedule`：查询当前用户自己的课表。
2. 接入 `query_my_attendance`：查询当前用户自己的考勤。
3. 接入 `query_my_leave`：查询当前用户自己的请假。
4. 接入 `query_announcements`：查询校园公告。
5. 增加基础自然语言路由：
   - “我的课表 / 今天有什么课 / 明天有什么课”
   - “我的考勤 / 考勤记录”
   - “我的请假 / 请假进度”
   - “公告 / 通知”

### 安全边界

- 所有工具仍必须先经过 `CampusAgentExecutor` 权限校验。
- 查询结果限制条数，避免一次性返回过多数据。
- 本轮不开放新增、修改、删除。

### 已完成

1. 新增 `app/services/campus_agent/tool_handlers.py`：
   - `query_my_schedule`
   - `query_my_attendance`
   - `query_my_leave`
   - `query_announcements`
2. 更新 `CampusAgentExecutor`：
   - 已登记 handler 的工具会真实执行查询。
   - 未登记 handler 的工具继续返回安全提示。
3. 更新 `/api/v1/campus-agent/chat`：
   - 在 `auto` 和 `academic_tools` 模式下识别低风险查询意图。
   - 自动路由到对应工具。
   - 返回自然语言摘要和 `tool_calls` 执行结果。
4. 增加基础参数归一：
   - “今天/明天有什么课”会转换为对应星期。
   - “待审批/已通过/驳回/请假考勤”等词会转换为基础状态过滤。

### 验证结果

- 后端 `py_compile` 通过。
- 前端 `npm run build` 通过。
- `student01` 工具级验证通过：
  - 可以查询自己的课表，并返回课程、教师、教室。
  - 可以查询自己的考勤。
  - 可以查询自己的请假。
  - 可以查询公告。
- `admin` 工具级验证通过：
  - 可以查询公告。
  - “我的课表/考勤/请假”因 admin 未关联学生或教师档案，返回空结果，符合当前业务规则。

### 遗留问题

- 当前仍是规则意图路由，尚未接入 LangGraph 编排。
- 当前只接低风险查询，提交请假和管理类查询尚未接入。
- 当前会话短期记忆还没有持久化到 `agent_session/agent_message`。

## 2026-06-26：足球助手短期记忆与连续追问开发

### 本次目标

- 让足球助手具备第一版短期记忆。
- 支持基于上一轮工具的连续追问，例如：
  - “那明天呢”
  - “只看请假的”
  - “再看公告”
  - “上一条详细点”
- 第一版先复用已有 `conversation` 表，不新增迁移，降低对当前系统的影响。

### 设计决策

1. 第一版短期记忆复用 `conversation.messages`：
   - 存储最近若干轮用户消息和助手回复。
   - 额外记录 `intent`、`tool_code`、`tool_args`、`tool_status`。
   - 用于下一轮判断用户是否在追问上一轮结果。
2. 暂不把长期记忆写入 Milvus：
   - 本轮只做会话级短期记忆。
   - Milvus 长期记忆留到后续 `agent_memory` 阶段。
3. 不改变权限规则：
   - 即使追问命中了上一轮工具，也必须重新走 `CampusAgentExecutor` 权限校验。
4. 后续迁移方向：
   - 将 `conversation` 迁移为 `agent_session` + `agent_message`。
   - 将长期偏好、学习状态、常用任务写入 `agent_memory` 和 Milvus。

### 开发步骤

1. 新增校园助手会话记忆服务。
2. `/campus-agent/chat` 读取或创建会话。
3. 每次工具调用成功或失败后写入短期消息。
4. 当用户没有明确工具意图时，尝试从上一轮工具上下文推断。
5. 清空对话时同时清理该会话短期记忆。

### 安全边界

- 短期记忆只辅助理解，不作为权限依据。
- 权限、角色、业务数据仍实时查数据库。
- 不在短期记忆里额外保存密码、身份证等敏感字段。

### 已完成

1. 新增 `app/services/campus_agent/memory.py`：
   - 创建或读取校园助手会话。
   - 写入用户消息和助手回复。
   - 保存上一轮工具上下文。
   - 支持清空当前会话短期记忆。
2. 更新 `/api/v1/campus-agent/chat`：
   - 每轮聊天都会绑定 `session_id`。
   - 工具调用结果会写入短期记忆。
   - 普通 AI 回复也会写入短期记忆。
3. 增加连续追问推断：
   - 上一轮查课表后，支持“那明天呢 / 今天呢”。
   - 上一轮查考勤后，支持“只看请假的 / 那考勤呢”。
   - 上一轮查请假后，支持“只看待审批 / 已通过的呢”。
   - 上一轮查公告后，支持“再看公告 / 上一条详细点”。
4. 新增 `POST /api/v1/campus-agent/clear`：
   - 清空当前足球助手会话记忆。
5. 前端清空按钮改为调用校园助手清空接口。

### 验证结果

- 后端 `py_compile` 通过。
- 前端 `npm run build` 通过。
- 使用 `student01` 进行两轮模拟验证：
  - 第一轮：“我的课表” -> 命中 `query_my_schedule`。
  - 第二轮：“那明天呢” -> 复用上一轮工具上下文，继续命中 `query_my_schedule`。
  - 清空会话接口执行成功。

### 遗留问题

- 当前短期记忆仍复用 `conversation` 表，后续应迁移为专用 `agent_session/agent_message`。
- 当前追问推断仍是规则版，后续应接入 LangGraph 节点。
- 当前没有长期记忆和 Milvus 写入，后续再接 `agent_memory`。
